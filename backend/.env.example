# ==============================================================================
# REQUIRED CONFIGURATION
# ==============================================================================

# Database Configuration (REQUIRED)
# For local development with Docker:
DATABASE_URL=postgresql+asyncpg://sigmasight:sigmasight_dev@localhost:5432/sigmasight_db

# JWT Configuration (REQUIRED)
# Generate with: openssl rand -hex 32
SECRET_KEY=your_secret_key_here_generate_with_openssl_rand_hex_32

# Market Data Providers (REQUIRED)
# ------------------------------------------------------------------------------
# FMP (Financial Modeling Prep) - REQUIRED
# This is the primary data source for all stock/ETF prices
# Get free API key at: https://site.financialmodelingprep.com/developer/docs
FMP_API_KEY=your_actual_fmp_key_here

# POLYGON_API_KEY - REQUIRED in config but can use dummy value if not needed
# Used as backup data source and for options (if available)
# Get free API key at: https://polygon.io/
# Note: Can use "dummy_key_not_used" if you only have FMP
POLYGON_API_KEY=your_polygon_api_key_here

# ==============================================================================
# OPTIONAL CONFIGURATION
# ==============================================================================

# Backup/Additional Data Providers (OPTIONAL)
# ------------------------------------------------------------------------------
# TradeFeeds - Backup data source (OPTIONAL)
# Only used if FMP fails
TRADEFEEDS_API_KEY=your_actual_tradefeeds_key_here

# FRED (Federal Reserve Economic Data) - Interest Rates (OPTIONAL)
# Used for risk-free rate in options calculations
# Get free API key at: https://fred.stlouisfed.org/docs/api/api_key.html
FRED_API_KEY=your_actual_fred_key_here

# Application Settings (OPTIONAL - These are the defaults)
# ------------------------------------------------------------------------------
DEBUG=false
ENVIRONMENT=development
LOG_LEVEL=INFO

# ==============================================================================
# NOTES FOR SETUP
# ==============================================================================
# 1. For minimal setup, you need:
#    - DATABASE_URL (use the default for Docker)
#    - SECRET_KEY (generate a new one)
#    - FMP_API_KEY (required for market data)
#    - POLYGON_API_KEY (can use "dummy_key_not_used" if only using FMP)
#
# 2. For full functionality:
#    - FMP_API_KEY for stock/ETF prices (primary)
#    - POLYGON_API_KEY for backup and options data
#    - FRED_API_KEY for interest rates
#
# 3. API Key Resources:
#    - FMP Free Tier: 250 requests/day
#    - Polygon Free Tier: 5 requests/minute
#    - FRED: Unlimited free tier
#    - TradeFeeds: Various paid tiers
#
# 4. For demo/testing:
#    - You can use dummy values for POLYGON_API_KEY if using FMP
#    - The system will work with just FMP_API_KEY configured
# ==============================================================================
