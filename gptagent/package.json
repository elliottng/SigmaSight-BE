{
  "name": "sigmasight-gpt-skeleton",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r --parallel run build",
    "dev": "pnpm --filter @sigmasight/api dev",
    "test": "pnpm -r --parallel run test"
  }
}
